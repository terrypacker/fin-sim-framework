import { hostPanePlugin } from './host-pane-plugin.js';

/** Displays the `#configGroupNodes` host; the runtime owns it. See `hostPanePlugin`. */
export const ConfigListPlugin = hostPanePlugin('configGroupNodes');
