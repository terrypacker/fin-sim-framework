import { hostPanePlugin } from './host-pane-plugin.js';

/** Displays the `#graphNodeLineagePane` host; the runtime owns it. See `hostPanePlugin`. */
export const LineagePlugin = hostPanePlugin('graphNodeLineagePane');
