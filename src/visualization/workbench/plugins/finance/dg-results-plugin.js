import { hostPanePlugin } from './host-pane-plugin.js';

/** Displays the `#dgResultsPane` host; the runtime owns it. See `hostPanePlugin`. */
export const DgResultsPlugin = hostPanePlugin('dgResultsPane');
